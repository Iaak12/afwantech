import UniversalServiceWrapper from "../services/UniversalServiceWrapper";

const PSDToHTML = () => {
  return <UniversalServiceWrapper slug="psd-to-html" pageName="PSD To HTML" />;
};

export default PSDToHTML;
